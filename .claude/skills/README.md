# Claude Code skills

These skill folders are generated local links to
[`.agents/skills/`](../.agents/skills/). They are gitignored so Windows
checkouts do not store broken git symlinks.

`npm ci` / `npm install` create them via `prepare`. If a Claude skill folder
is missing (clone without install), run:

```bash
npm run skills:link
```
